server:
  port: 8086

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url:
    username:

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
    show-sql: true